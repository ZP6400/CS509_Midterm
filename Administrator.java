//The Administrator class, which extends the User class
public class Administrator extends User {

    //The constructor for the Administrator class
    public Administrator(String login, String pin_code) {

        super(login, pin_code);
    }
}
